function ArithGeoII(arr) { 
	for(var i = 0 ; i < arr.length-1;i++){
    	if(arr[i+1]-arr[i] != arr[arr.length-1-i]-arr[arr.length-2-i]){
          	for(var k = 0; k < arr.length-1; k++){
        	 if(arr[k+1]/arr[k] != arr[arr.length-1-k]/arr[arr.length-2-k]){
             	return -1
             }
           } return "Geometric"
        }
    }  return "Arithmetic"   
}
   
